# browser_oxide_compare Turnstile 推进记忆（2026-09-05）

## 工作区状态

- 仓库：`/Users/lichangqing/Copilot/gpt/browser_oxide_compare`
- 当前分支：`merge/silvr-integration`
- 当前 HEAD：`8b3fe47`
- 推送目标：`fork/merge/silvr-integration`
- 上游参考仓库：`/Users/lichangqing/Copilot/gpt/.scratch/github/yfedoseev__browser_oxide`
- 上游参考 ref：`main@9cd4af3`

## 本轮已完成并推送

1. `c2996b4 fix: improve browser API and worker fidelity`
   - Trusted Types、跨域 frame CSP、Worker/MessageChannel/CacheStorage、动态图片解码、Canvas/ImageBitmap 像素、Navigation API、PerformanceObserver、ICE 事件等。
2. `b98182b fix: align WebRTC interface surfaces`
   - 对齐 Chrome 148 WebRTC 原型与实例属性形状。
3. `1aefdad fix: match Chrome navigator surface`
   - Navigator 与 Chrome 148 对齐为 81/81，无缺失或多余属性。
4. `46877f7 chore: filter fetch diagnostics`
   - `BROWSER_OXIDE_FETCH_TRACE` 支持按 URL 子串过滤诊断日志。
5. `8b17e08 fix: match Chrome event WebIDL surfaces`
   - Event、CustomEvent、MessageEvent、MouseEvent、EventTarget 的 WebIDL 表面、属性顺序和 descriptor 对齐 Chrome 148。
6. `8b3fe47 fix: route IPv6-only challenge shards`
   - `*.challenges.cloudflare.com` 在仅返回 IPv6、主机无 IPv6 路由时回退到双栈边缘，同时保留原 URL、SNI 和 Host。

## 验证证据

- `cargo test -p browser_oxide --test chrome_compat -- --test-threads=1`
  - `444 passed; 0 failed; 8 ignored`
- `cargo clippy -p browser_oxide --lib --tests -- -D warnings`
  - 通过。
- 官方 Cloudflare always-pass 页面：
  - 日志：`.scratch/widget_live_after_event_surface_20260905.log`
  - 成功得到测试 token：`XXXX.DUMMY.TOKEN.XXXX`
- x.ai 生产挑战：
  - 日志：`.scratch/xai_minimal_dns_fallback_20260905.log`
  - 仍为 `TOKEN_FOUND=false`，不得宣称生产 token 已成功。
  - DNS 修复后，Worker 对 `brunhild.challenges.cloudflare.com` 的探针已从 DNS error 推进到 HTTP 204，并收到 `cf-chl-peek`。
  - 首个 `POST /fo` 返回 HTTP 200，响应体约 846128 字节；`/ci` 图片已获取并解码。
  - 关键差距：BrowserOxide 未进入 Chrome 148 随后会发送的多轮 `/fo` 序列。
- Chrome 148 对照：
  - `.scratch/chrome_turnstile_cdp.json`
  - `.scratch/chrome_turnstile_cdp_run2_stdout.json`
  - 同一 bare headless 页面约 36 秒内也未得到生产 token，但会继续发出多次 `/fo`，可作为下一断点。

## 当前判断与下一步

- 浏览器基础表面与挑战网络路径的两个主要阻塞已基本拆除。
- 下一步不再泛补 API，集中追踪首个 `/fo` 响应后的挑战脚本执行、定时器/Promise/事件及交互状态机，找出为何没有第二个 `/fo`。
- 建议把 BrowserOxide 与 Chrome 的网络时间线、响应执行点和 JS 异常按同一时间轴差分，并将差异固化为最小回归测试。

## 不应误提交的现有文件

以下为用户改动、诊断探针或本地产物；后续提交必须继续按子任务精确 stage：

- `AGENTS.md`
- `crates/browser_oxide/examples/xai_signup_probe.rs`
- `.scratch/`
- `crates/browser_oxide/examples/global_surface_probe.rs`
- `crates/browser_oxide/examples/xai_turnstile_clean.rs`
- `crates/browser_oxide_py/target/`
